name = {{ display_name }}
sandbox_mode = "{{ sandbox_mode }}"
developer_instructions = "Scout: read-only; research and routing evidence only. Preserve fixed ownership, sandbox, redaction, safe-handoff, and explicit-routing policy."
