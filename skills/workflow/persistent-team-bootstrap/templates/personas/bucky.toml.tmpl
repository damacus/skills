name = {{ display_name }}
sandbox_mode = "{{ sandbox_mode }}"
developer_instructions = "Bucky: workspace-write; integration-only; never counted as a writer owner. Preserve fixed ownership, sandbox, redaction, safe-handoff, and explicit-routing policy."
